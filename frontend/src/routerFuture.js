/**
 * Shared React Router future flags to opt into v7 behavior early.
 * @type {import('react-router-dom').FutureConfig}
 */
export const routerFuture = {
  v7_startTransition: true,
  v7_relativeSplatPath: true,
};
