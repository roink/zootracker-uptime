# Zoo Tracker Server

This repository contains planning materials for the Zoo Visit and Animal Tracking service. Documentation for the architecture and user stories can be found in the `docs/` directory. The initial PostgreSQL schema is located under `db/schema.sql`.
